# COURTSIDE 개발 룰

## 절대 원칙

1. **요청하지 않은 변경 금지**: 요청한 작업만 정확히 수행한다. 기존 UI, 레이아웃, 로직을 임의로 변경하지 않는다. 임의 변경은 로직 꼬임의 원인이 된다.

2. **문서 선행 업데이트**: 모든 변경/수정사항은 프로토타입 작업 전에 관련 문서(요구사항, 화면설계서, 비즈니스 로직 등)를 먼저 업데이트한다.

3. **push 전 최신 동기화**: GitHub API로 push 하기 전 반드시 `git fetch origin main && git reset --hard origin/main`으로 최신 상태를 반영한 후 작업한다. 다른 세션의 커밋을 덮어쓰지 않는다.
   - push 직전에 GitHub API에서 해당 파일의 최신 내용을 다운로드하여 그 위에 수정한다. 로컬에 캐시된 오래된 파일을 push하지 않는다.
   - 세션 시작 시 `git log`로 마지막 커밋 확인. 다른 세션(데스크탑 등) 커밋이 있으면 반드시 전체 반영 후 작업 시작한다.
   - 데스크탑과 웹 세션에서 동일 파일을 동시에 작업하지 않는다.

4. **기존 디자인 존중**: 버튼 배치, 컬러, 레이아웃 등 기존에 확정된 디자인은 명시적 요청 없이 변경하지 않는다. 추가 요청 시 기존 구조를 유지하면서 추가만 한다.

5. **완료 전 자체 테스트 필수**: 작업 완료 보고 전에 반드시 Playwright 등으로 스크린샷을 찍어 인터랙션이 정상 동작하는지 직접 확인한다. HTML/JS 추가 시 함수 존재 여부, 바텀시트 노출, 버튼 클릭 동작을 검증한 후에만 완료 보고한다. 테스트 실패 시 수정 후 재테스트한다.

6. **화면 보고 시 링크 필수**: 화면 스크린샷을 보여줄 때 반드시 GitHub Pages 링크를 함께 제공한다. 사용자가 직접 모바일에서 확인할 수 있도록 한다.

## 개발 작업 전 필수 참고 문서

모든 개발 작업(프로토타입 수정, 프론트엔드, 백엔드)은 반드시 아래 문서를 먼저 읽고 이해한 후 진행한다. 문서에 정의된 스펙과 다르게 구현하지 않는다.

### 참고 순서

| 순서 | 문서 | 경로 | 내용 |
|------|------|------|------|
| 1 | 요구사항 정의서 | `docs/01-plan/features/tennis-coach-matching.plan.md` | 48개 FR 요구사항, 기능 범위 |
| 2 | 화면설계서 | `docs/02-design/screen-spec.md` | 전체 화면 스펙, 인터랙션, API 호출 정의 |
| 3 | DB 스키마 | `docs/02-design/database-schema.md` | 37개 테이블 정의 (Prisma/PostgreSQL) |
| 4 | 비즈니스 로직 | `docs/02-design/business-logic.md` | 알고리즘, 정책, 상태 관리 규칙 |
| 5 | 디자인 가이드 | `docs/02-design/design-guide.md` | 컬러, 타이포, 컴포넌트, 스페이싱 |
| 6 | 알림톡 템플릿 | `docs/02-design/alimtalk-templates.md` | 18개 카카오 알림톡 메시지 템플릿 |
| 7 | HTML 프로토타입 | `docs/03-prototype/` | 51개 화면 프로토타입 (동작 참고) |

### 규칙

- 기능 개발 시 해당 FR 번호와 screen-spec의 화면 번호를 먼저 확인한다.
- DB 스키마에 정의된 테이블/필드를 기준으로 구현하고, 스키마에 없는 필드를 임의로 추가하지 않는다.
- 비즈니스 로직 문서에 정의된 정책(회차 계산, 결제 상태, 변경 요청 플로우 등)을 그대로 따른다.
- 디자인 가이드의 컬러/타이포/컴포넌트 스펙을 준수한다.
- 프로토타입 HTML의 인터랙션을 참고하되, 스펙 문서가 우선한다 (충돌 시 문서 기준).

## 현재 작업 상태 (새 세션에서 이 섹션만 읽으면 바로 이어 작업 가능)

### 프로젝트 개요
- **COURTSIDE**: 테니스 코치 SaaS (Coach가 본인 학생 관리)
- **Phase 1** (기획+설계+프로토타입): 완료
- **Phase 2** (피벗 2026-05-08): 마켓플레이스 → Coach SaaS 전환 완료
- **현재 작업**: **개발 단계 진입 — Sprint 1 (Next.js 셋업 + 인증 + 프로필)**

### 개발 결정사항 (확정)
- **기술 스택**: Next.js 14 (App Router) 단독 full-stack — `Server Actions` + `Route Handlers`
- **DB/Auth/Storage/Realtime**: Supabase (PostgreSQL + Prisma ORM)
- **결제**: 토스페이먼츠 (코치 구독료)
- **알림톡**: Solapi (카카오 비즈채널)
- **배포**: Vercel
- **상세**: `docs/01-plan/development.plan.md` 참고

### 주요 문서
- 개발 계획: `docs/01-plan/development.plan.md` (Coach SaaS 기준 새 Sprint 1~4)
- 기존 plan: `docs/01-plan/features/tennis-coach-matching.plan.md` (마켓플레이스 Sprint Plan은 deprecated)
- DB 스키마: `docs/02-design/database-schema.md`
- 화면 스펙: `docs/02-design/screen-spec.md`
- 프로토타입: `docs/03-prototype/` (개발 중 레퍼런스로 보존)

### GitHub
- main 브랜치 직접 push
- GitHub Pages → 프로토타입 미리보기용 (개발 진입 후에도 유지)

### Sprint 1 다음 태스크 (순서대로)
1. Next.js 14 프로젝트 셋업 (`app/`, TypeScript, Tailwind, App Router)
2. Prisma + Supabase 연결 + `schema.prisma` 작성
3. 카카오/네이버 소셜 로그인 (Supabase Auth)
4. FR-01b 역할 선택 + FR-01c/02 프로필 등록
5. FR-02b 코치 스케줄 등록
6. 공용 레이아웃 + 네비바 (코치 5탭 / 학생 4탭)

### 개발 완료 (실데이터 연동)
- **코치 마이페이지** (`app/my/page.tsx` 코치 분기, 프로토타입 7-1 기반): 프로필 헤더(아바타·플랜 배지 FREE/PRO·인증·평점), 통계 3종(이번주 레슨/대기중 신청/등록 학생), 메뉴 리스트(내 프로필 관리·구독 플랜·학생 등록 요청·전체 스케줄 / 공지·리뷰·통계·알림·계정은 준비 중 칩). 학생 분기는 기존 placeholder 유지.
- **내 프로필 관리** (`app/my/profile/`): 코치 프로필 조회+수정 폼(이름/생년월일/성별/자기소개/활동지역/경력). `updateCoachProfile` 서버 액션으로 `coach_profiles`·`users` 갱신. 이메일·전화번호는 읽기 전용(마스킹).

### 완료된 프로토타입 (참고용)
1. 코치 스케줄 홈 (7-0): 주간 캘린더, 상태별 카드 8종, 필터 칩, 레슨 상세 풀팝업, 상태처리 바텀시트 (완료/보강/결강/결제확인), 변경요청·대기신청 상세
2. 학생 스케줄 홈 (6-0): 코치 공지·응답필요(보강/변경/리뷰/재등록 4종) 그룹 박싱, 카운트다운 pill 3줄 카드, 슬롯 시트 + 코치 문의하기
3. 결제 표시 (6-6): 결제상태 뱃지 URL 파라미터, 예약금/잔액 분리 표시
4. 회원 검색 (7-10): 실시간 검색 + 4종 정렬
5. 코치 전용 5탭 네비바 통일 (9개 화면)
6. 상태 흐름도 (표/차트 모드 전환)

### 보류 항목 (Phase 2 또는 개발 중 처리)
- 결강처리 바텀시트 UI 개선 (보강처리와 동일 패턴으로) — 개발 중 처리
- 수강생 화면 추가 프로토타입 고도화 — 개발하면서 보강
